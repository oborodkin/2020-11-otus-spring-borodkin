spring:
  datasource:
    url: jdbc:h2:mem:elib
    schema: schema-main.sql
    data: data-main.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true

